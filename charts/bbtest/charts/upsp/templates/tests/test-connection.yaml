apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upsp.fullname" . }}-test-connection"
  labels:
{{ include "upsp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "upsp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
