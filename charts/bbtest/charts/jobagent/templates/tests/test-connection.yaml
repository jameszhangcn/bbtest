apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jobagent.fullname" . }}-test-connection"
  labels:
{{ include "jobagent.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jobagent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
